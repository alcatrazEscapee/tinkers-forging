v1.0.7

Changes:
 - Fixed a bug where switching between worlds could cause a tick to happen without proper temperature updates, resulting in items melting in forges when they shouldn't in some edge cases.